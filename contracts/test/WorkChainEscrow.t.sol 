// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "forge-std/Test.sol";
import "../src/WorkChainEscrow.sol";

contract WorkChainEscrowTest is Test {
    WorkChainEscrow escrow;
    address usdc = address(0x1);
    address client = address(0x2);
    address freelancer = address(0x3);

    function setUp() public {
        escrow = new WorkChainEscrow(usdc);
    }

    function test_BuilderCode() public view {
        assertEq(escrow.BUILDER_CODE(), "bc_v1ampve4");
    }
}